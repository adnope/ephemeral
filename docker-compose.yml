services:
  ephemeral:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ephemeral-data:/app/data
    environment:
      - PORT=8080
      - DATA_DIR=/app/data
      - SESSION_SECRET=${SESSION_SECRET:-change_me}
    restart: unless-stopped

volumes:
  ephemeral-data:
